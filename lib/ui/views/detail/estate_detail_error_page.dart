import 'package:flutter/material.dart';

class EstateDetailErrorPage extends StatelessWidget {
  const EstateDetailErrorPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
